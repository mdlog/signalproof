<!-- trace: idea="SignalProof — verifiable connectivity data for DePIN: phone measurements committed on Ethereum Sepolia, proven by the Attestcoin Protocol, settled and rewarded on Creditcoin CC3 Testnet." | event="BUIDL CTC 2026 Fall" | deadline="2026-09-13 23:59 ET (2026-09-14 03:59 UTC) — re-verified live 2026-09-11 09:36 UTC: payload timelineEnd=1789358340, isExtended=true" | source=submission-packager -->
# Video script — target 3:00 (no organizer limit published; self-imposed cap 3:30)

**Organizer rule, quoted from the brief:** "Demo-video max length + format rules:
`DATA TIDAK DITEMUKAN` — none stated; a URL is all the form takes." The cap below is ours: judges
are screening 87 entries, and a 3-minute video that shows the settlement in the first 10 seconds
beats a 7-minute tour.

**Ground rules for the edit**
- The wow comes first. Do not open with a logo, a name, or "Inspiration".
- Every explorer link shown is a real transaction from `README.md`; the on-camera measurement is real; the settlement that flips on camera is the pre-warmed one from `docs/DEMO_RUNBOOK.md`. If the pre-warm does not settle inside the take, the voiceover says so (shot 7 alt) — never pass an old row off as new.
- On-screen text is lower-third mono, max 9 words. Speak at ~150 wpm; the voiceover below is ~413 words, which is about 2:45 of speech, leaving room for the two beats where the screen speaks for itself.
- Take order is not edit order. `docs/DEMO_RUNBOOK.md` records the shots in the order the screen can produce them (wow window 1:30–2:00 of the take); the timings in this table are the **edit**, where the settlement flip is also used as the cold open.
- Record the screen at 1920×1080, browser zoom 110–125%, cursor visible. Face cam is optional and only in shots 2 and 10.

## Shot list

| Time | Shot (screen / face / slide) | What to click / show | On-screen text | Voiceover (verbatim) |
|---|---|---|---|---|
| 0:00–0:10 | **Screen — cold open.** Proof queue, a card flipping from amber "Awaiting attestation" to teal "Settled" (use the backup recording of a real flip if the live one lands later). Immediately cut to its Blockscout page, **Logs** tab, `MeasurementVerified` expanded. | Pre-opened tab: `https://creditcoin-testnet.blockscout.com/tx/0x26c87ed0d7dda10b864c7a6907dad4c52d19d2f449ff73eabc4db099736d5c13` | `A phone measurement, proven on Creditcoin` | "This is a phone's network measurement being paid for by a contract on Creditcoin. The contract never trusted the phone, our server, or the relayer. It trusted a proof." |
| 0:10–0:30 | **Slide (or face).** The README pipeline diagram: phone → gateway → Sepolia → Attestcoin → Creditcoin → claim(). | Static slide, one highlight per chain as named | `SignalProof — verifiable connectivity data for DePIN` | "Anyone can claim coverage numbers. Nobody can prove them. SignalProof commits a measurement as a hash on Ethereum Sepolia, proves it exists there with the Attestcoin Protocol, and settles it on Creditcoin, where the contract checks the cross-chain proof and pays the contributor. Here is the whole thing, live on testnet." |
| 0:30–1:05 | **Screen — live measurement.** Header **Connect wallet** → approve. Sidebar **Run a test**. Click **Run valid test** → allow location → cards fill → sign the wallet prompt → **Awaiting attestation…** | Show the four cards filling: **Coarse area** (geohash, "coordinate discarded"), **Latency** ("median of 7 round trips"), **Throughput** ("3.00 MB over Ns"), **Network class**. Then **What the app sends** JSON. | `Measured, not asked · geohash-6 · coordinate discarded` | "Run a test measures rather than asks. Latency is the median of seven timed round trips. Throughput streams three megabytes — the browser's own estimate under-reported a forty-megabit link as seven. The area is a six-character geohash, roughly a kilometre by six hundred metres; the coordinate never leaves the function. The wallet signs the measurement root. It pays nothing — the relayer pays gas on both chains." |
| 1:05–1:25 | **Screen — Sepolia.** Wait for the label **"N blocks to attestation · ~M min"** and the **attested height / your block** box. Sidebar **Proof queue** → click the new **Awaiting attestation** card → Etherscan. | Etherscan tx page of the new commit: `submitMeasurement` from the relayer to `0x15F3d74846a40bD67f8ce345B73ae4c400f759Dc`; **Logs**: `MeasurementSubmitted`, `contributor` = the connected wallet | `Sepolia: relayer-gated registry · contributor = topics[3]` | "Fifteen seconds later it is on Sepolia. The registry accepts one relayer — the address that carries our gateway's signature check on-chain. Creditcoin attests Sepolia about ten blocks every two minutes, so this settles in roughly eight minutes. That wait is the protocol working, and every number here is checkable on a Sepolia explorer." |
| 1:25–1:55 | **Screen — code, then Blockscout.** 10 s on `contracts/src/SignalProofSettlement.sol` lines with `SourceTransactionFailed` and `WrongEmitter`. Then the batch transaction. | Code: the two `revert` lines. Then `https://creditcoin-testnet.blockscout.com/tx/0xcf1a01c55052ef604494ee53c717f9401019d7fb56c6681fb1e11f2559c49598` **Logs**: three `MeasurementVerified` + one `BatchSettled` | `Precompile proves inclusion. Not success. Not the emitter.` | "On Creditcoin, our contract inherits ASCBase and overrides one hook. The BlockProver precompile proves inclusion in an attested block — and only that. It does not prove the transaction succeeded, and it does not prove which contract emitted the log. We check both, because without the second, a lookalike contract on Sepolia could drain the pool. The docs' example skips it; we didn't. And here is the batch route: three measurements, one continuity proof, one Creditcoin transaction." |
| 1:55–2:20 | **Screen — WOW.** Back to **Proof queue**. The pre-warmed card is now **Settled** (badge count on the nav item, teal check). Click it → Blockscout `execute()` on `0x8F14…b584`. Then the **Contributor reward** card: accrued balance → **Claim 0.001 CTC** → confirm in wallet → **Claim sent — view on Blockscout**. | Live. If the pre-warm has not settled yet, see 7-alt. | `Proven · paid · claimed by the contributor's own wallet` | "And the measurement we started ten minutes before recording has just settled. Proof verified, reward accrued. Claim is the only transaction the contributor ever signs — the reward moves from the pool to their wallet on Creditcoin." |
| 1:55–2:20 **alt** | If the pre-warm has not settled inside the take: show an existing **Settled** card and its Blockscout page, and the earlier recorded claim. | `https://creditcoin-testnet.blockscout.com/tx/0x14853610d5d9ae99ade6fdca0cb3c0ead461d29baf252b18c2cfa48b83fe0030` (pool 5.000 → 4.999 CTC) | `Recorded earlier · settled 8.5 min after commit` | "The measurement we submitted before recording is still a few blocks from attestation, so here is one that settled earlier: proof verified, reward accrued, and the claim that moved it from the pool to the contributor's wallet. Total, eight and a half minutes from phone to claimable reward." |
| 2:20–2:40 | **Screen — Trust boundary.** Right column of Proof queue. Read the three rows. Optional 6 s: **Run a test** → **Demo: submit a tampered payload** → red **MEASUREMENT_ROOT_MISMATCH** message. | Trust boundary rows link to the contracts; tamper demo message | `Two findings kept as inverted exploit tests · tamper = real gateway rejection` | "Two things broke during the build and are kept as tests. The first registry was permissionless — anyone could name themselves payee and be paid for nothing; it is now relayer-gated. The batch route let one measurement be paid twice; it now defers to its sibling. Sixty-seven Foundry tests, ninety-three Vitest tests." |
| 2:40–2:54 | **Screen — Coverage overview, then Data products.** The map with decoded geohash cells; **Weakest zones**; then the area cards and the footer "the buyer API is not built". | Map subtitle "aggregated from on-chain submissions" | `Every cell is a decoded geohash · nothing invented` | "Every settled area is drawn as the cell its geohash covers. The buyer API is not built yet, and the screen says so. Nothing here is a fixture — it is a join of the two chains' logs." |
| 2:54–3:00 | **End card.** Repo URL, one-liner, three contract addresses, "what's next". | github.com/mdlog/signalproof | `SignalProof · DePIN · BUIDL CTC 2026 Fall` | "Next: native client, anti-Sybil, buyer API, signature on-chain. SignalProof — verifiable connectivity data, settled on Creditcoin." |

**Running time:** 3:00 (2:54 + end card). Confirm the export is ≤ 3:30 before uploading; write the
final length here after the edit: `___:___`.

## Explorer links to have open (in this order, one tab each)

1. Registry on Sepolia — `https://sepolia.etherscan.io/address/0x15F3d74846a40bD67f8ce345B73ae4c400f759Dc`
2. Settlement on CC3 — `https://creditcoin-testnet.blockscout.com/address/0x8F14B2cC1b807203d332DE6E3DA6274176FDb584`
3. Batch settlement on CC3 — `https://creditcoin-testnet.blockscout.com/address/0x3B90e22f246bBa68f6de682b564c33b121D68C85`
4. Complete single run — commit `https://sepolia.etherscan.io/tx/0x22474613e60df0650456a3ff9fd93211b833e70bbe8ef3910238e30eb82368f7` → settle `https://creditcoin-testnet.blockscout.com/tx/0x26c87ed0d7dda10b864c7a6907dad4c52d19d2f449ff73eabc4db099736d5c13` → claim `https://creditcoin-testnet.blockscout.com/tx/0x14853610d5d9ae99ade6fdca0cb3c0ead461d29baf252b18c2cfa48b83fe0030`
5. Batch run — `https://creditcoin-testnet.blockscout.com/tx/0xcf1a01c55052ef604494ee53c717f9401019d7fb56c6681fb1e11f2559c49598`
6. Gateway → chain with no database — `https://sepolia.etherscan.io/tx/0x17e8c0c317c034c73c5f7a5c7decd92df4c9c91371430af8d1ca7da325c90683` → `https://creditcoin-testnet.blockscout.com/tx/0x87bc9d13181e76c9dc250a64423f3d26ff2a4e4d425d9673b7cfd5aabf3d5ae4` (B-roll if a shot runs short)

Note for the edit: the transactions in 4 predate the gated registry redeploy (Sepolia block
11,658,403); the dashboard's history deliberately restarts at the gated registry. If you show 4 on
screen, the voiceover already calls it "one that settled earlier" — do not call it "today's".

## Optional B-roll (10–15 s, if a shot runs short)

Terminal, ≥ 16 pt font: `pnpm smoke` output — "resolved chainKey=1 for Sepolia", "lag behind
Sepolia head: N blocks (~M min)", "height … bracketed by parent … child … (attested=true)". It is
the fastest visual proof that the read path is real and needs no key.
